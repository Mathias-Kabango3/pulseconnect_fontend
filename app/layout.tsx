import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { AuthProvider } from "../context/AuthContext";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PulseConnect – Doctor Appointment Booking",
  description:
    "A seamless and efficient platform for booking doctor appointments, built with Next.js, TypeScript, and PostgreSQL.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-white min-h-[70vh] `}>
        <main className="relative">
          <AuthProvider>{children}</AuthProvider>
        </main>
        <Toaster position="top-right" />
      </body>
    </html>
  );
}
